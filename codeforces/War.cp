#include <bits/stdc++.h>
#include <iostream>
#include <set>

using namespace std;

int main()
{
    long long n,m,k;
    
    cin>>n>>m>>k;
    
    if(n<(m*k)){
      cout<<"King Canute\n";
    }else{
      cout<<"Iron fist Ketil\n";
    }
    
    return 0;
}
 	 	  		 		 		 	 		 	  	   	 	