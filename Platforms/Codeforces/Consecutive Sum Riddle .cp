#include <bits/stdc++.h>
#define optimized ios::sync_with_stdio(false); cin.tie(0);

using namespace std;

int main()
{
    optimized
    int t;
    cin>>t;
    while(t--){
      long long n;
      cin>>n;
      
      long long r,l;
      r=-(n-1);
      l=n;
      cout<<r<<" "<<l<<endl;
    }
        
  return 0;
}